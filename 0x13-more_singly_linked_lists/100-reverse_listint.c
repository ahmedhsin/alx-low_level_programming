#include "lists.h"
/**
 * reverse_listint - print func on linkedlist
 * @head: pointer to the head of list
 * Return: ptr -> head
*/
listint_t *reverse_listint(listint_t **head)
{
	listint_t *tmp = (*head);
	listint_t *tmpp;

	while (tmp->next)
	{
		tmpp = tmp->next;
		
	}
}

/*
 1->2->3->4->5->n
*/
